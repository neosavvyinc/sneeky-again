akka {
  loglevel = INFO
}

spray.can.server {
  request-timeout = 60s
  idle-timeout = 120s
}

com.phantom.ds {
  db.driver=com.mysql.jdbc.Driver
  db.url="jdbc:mysql://localhost/phantom_test"
  db.user="root"
  db.password=""

  db.statementCacheSize=10
  db.minConnectionsPerPartition=5
  db.maxConnectionsPerPartition=5
  db.numPartitions=1

  auth.secret="its a secret"
  auth.requestTimeout=60000
  auth.sessionTimeout=-1
  auth.mode=nohash

  twilio.accountSid="AC3efd7fc2d6356f8991d7d2e1f6beac6b"
  twilio.authToken="fce86014cca37b5f4161bb78990548bc"
  twilio.phoneNumber="+19194393955"

  apple.keyStorePassword="sneekyapp1"
  apple.messageBody="You got a new dick pic!"
  apple.certPath="src/main/resources/Certificates.p12"
  apple.environment="development"

  user.invitationMax=3
  user.maxRetries=3

  fileStore.baseDirectory="/tmp/phantom_test/"
  fileStore.baseImageUrl="http://api.phantom.neosavvy.com/images/"

  security.encryptFields=false
  security.sharedSecret="secretEncryption" //must be 16 bytes
}
